let dayTemp = [22,26,28,23,18,19,19];
function weatherWidget(showTemp,day) {
    let dayChooser = document.querySelector("select[name='day-select']");
    let showTemp = document.querySelector("span[id='temperature']");
    let showDay = parseInt(indexOF.dayChooser.value); 
    let showTemp = indexOF(dayTemp)

    showDay.innerHTML = showtemp
}